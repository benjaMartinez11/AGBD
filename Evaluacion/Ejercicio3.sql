-- 3) Buscar la lista de todas las personas
-- que NO están asociadas al gimnasio Get-Fit-Now

SELECT name 
WHERE name not in
FROM get_fit_now_check_in
WHERE get_fit_now_check_in like = 'Get-Fit-Now'
 